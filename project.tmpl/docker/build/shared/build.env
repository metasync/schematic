APP_SOURCE := $(shell git config --get remote.origin.url)
APP_AUTHORS := MetaSync

IMAGE_REPO_ROOT := quay.io/metasync
IMAGE_REPO := ${IMAGE_REPO_ROOT}/${PROJECT_NAME}-${APP_NAME}
IMAGE_TAG := ${APP_VERSION}-${RELEASE_TAG}
IMAGE_NAME := ${IMAGE_REPO}:${IMAGE_TAG}

IMAGE_REGISTRY_NAME := ${IMAGE_REPO}:${IMAGE_TAG}.${BUILD_NUMBER}
CREATED_AT := $(shell date -u +'%Y-%m-%dT%H:%M:%SZ')

BASE_IMAGE_NAME := schematic-base
BASE_IMAGE_VERSION := 0.1.0
BASE_IMAGE_RELEAST_TAG := rel
BASE_IMAGE_BUILD_NUMBER := 0
BASE_IMAGE_REPO := ${IMAGE_REPO_ROOT}/${BASE_IMAGE_NAME}
BASE_IMAGE_TAG := ${BASE_IMAGE_VERSION}-${BASE_IMAGE_RELEAST_TAG}.${BASE_IMAGE_BUILD_NUMBER}

CONTAINER_CLI := docker
# CONTAINER_CLI := podman
